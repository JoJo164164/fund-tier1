# 台灣基金滾動跌幅系統 — 專案憲法 v4
建立 2026-07-14｜更新 2026-07-20（BAC三階段建置完成，工具備齊待實跑建庫）
狀態：**系統骨架全到位(建庫腳本+每早掃描+四類資料源皆自測過)；下一步實跑建庫灌資料**

> **定位**：把股票版「滾動跌幅觸發→回測勝率→進場時機→追蹤日誌驗證」移植到基金。
> 價格/回測引擎複用母專案；體質評分待數據後再設計（使用者裁決暫緩）。

---

## ★ 第零：系統總藍圖（2026-07-20 使用者確認定案，最高層規格）★

### 三層架構（三種時間軸，不可混為一談）
| 層 | 用途 | 時間要求 | 頻率 |
|----|------|---------|------|
| **① 歷史庫** | 算歷史勝率、篩選基金 | **可跑數天、分批建**（斷點續傳） | 建一次+每晚補一天 |
| **② 每早自動掃描** | 早上打開直接看「今天誰觸發+歷史勝率」 | **前一晚自動跑好**，早上零等待 | 每交易日 |
| **③ 手動重掃+篩選** | 隨時縮範圍快速重掃 | 幾秒~幾分鐘 | 隨時 |

**關鍵認知（使用者點破）**：使用者「一早跑一下就要很快決定下單」→ 早上碰的只有 ②③，
必須快。① 歷史庫是背景先建好的參考值，**絕不佔用早上下單時間**。
之前讓使用者以為每天要等6小時是理解錯誤，已澄清。

### 四類標的（範圍不可縮減，鐵律11；三類資料源已實測可抓歷史）
| 類別 | 資料源 | 實測狀態（2026-07-20） |
|------|--------|----------------------|
| 被動ETF | yfinance | ✅ 母專案已驗+已部署 |
| 主動ETF | yfinance + SITCA | ✅（<3年資料，只收不出結論，見鐵律10/憲法九） |
| 境內基金（含投海外） | **SITCA IN2106** | ✅ 已建庫實測1438筆 |
| **真正境外基金** | **MoneyDJ** | ✅ 已實測抓到歷史淨值序列 |

### 篩選維度（③ 手動重掃用；分兩批上線）
**第一批（現有資料即可，先上）**：境內/境外、基金公司
**第二批（需查對照表，後補）**：股債平衡型（SITCA「基金類型代號說明」對照表）、
  投資市場（美國/亞洲/新興…，從名稱或分類欄）

---

## ★ 三大資料源實測結果（2026-07-20，全部 web_fetch 實證，非臆測）★

### 資料源1：SITCA IN2106（境內基金，含投海外）— ✅ 端到端可用
- URL：`https://www.sitca.org.tw/ROC/Industry/IN2106.aspx?pid=IN2213_02`
- **method=POST**，ASP.NET WebForms，回傳為純 HTML `<table>`（非XHR，直接在document內）
- **兩步式抓取**（token每次變，不可寫死）：① GET空頁取 `__VIEWSTATE`/`__EVENTVALIDATION`
  → ② 帶token+日期+公司 POST → 解析回傳table
- 欄位名（真實VIEWSTATE解碼確認）：日期=`ctl00$ContentPlaceHolder1$txtQ_Date`(YYYYMMDD)、
  公司=`ctl00$ContentPlaceHolder1$ddlQ_Comid`（值為純代碼A0005）
- **一次POST回「某投信某日全部基金」**（橫切面）：ETF+共同基金+境內投海外全混在一起回
- 36家投信代碼↔名稱對照：已從VIEWSTATE完整解出（見下方附錄）
- **解析用兩段式**（先切`<tr>`再抽`<td>`文字）— 教訓：SITCA實際回傳用**雙引號**
  `<td align="left">`，但VIEWSTATE內是單引號 → 寫死引號的正則只命中9/200列。
  兩段式不受引號/class/長名稱影響，已用三張真實DOM圖驗證全中。
- **已建GitHub Actions建庫**：斷點續傳+禮貌延遲+CSV增量，安聯野村5日實測1438筆✅
- **境外基金淨值有時差**（使用者提供）：隔天下午4點後才陸續公告 → 抓太新的日期會殘缺
  （07/17安聯掉到8檔、野村30檔，前4天正常119/231）。
  **使用者裁決：不猜「跳幾天」，改為「抓當下最新NAV、是哪天算哪天、標示NAV日期」**
  （呼應鐵律16淨值截至日）。

### 資料源2：MoneyDJ（真正境外基金）— ✅ 實測抓到歷史淨值
- **清單頁**：`https://www.moneydj.com/funddj/ya/yp081001.djhtm`
  → 傳統server-render、Big5編碼、全市場境外基金+當前淨值，每檔連結帶代碼 `?a=XXXX`
- **單檔淨值頁**：`https://www.moneydj.com/funddj/ya/yp010001.djhtm?a={代碼}`
  → ✅ 實測摩根印度(a=jfz14)：HTML內含「最近30日淨值」表(日期→淨值)
     + 最高/最低淨值(年) → 有年度以上歷史深度
- **不是SPA、不擋爬蟲**（實測可fetch），與各總代理官網(SPA/擋bot)相反 → 選它當集中點正確
- ⚠️ **待確認**：預設顯示30日，15年歷史怎麼抓（`yp011001GIF`走勢圖端點或分頁參數）未挖到底
- 教訓：**「境內基金投海外」≠「真正境外基金」**。SITCA的「野村環球/美國」是野村投信發行
  （境內基金投海外，在IN2106）；真正境外基金是國外公司在盧森堡/愛爾蘭註冊、總代理引進
  （不在IN2106，在MoneyDJ/境外基金觀測站）。使用者要「三者全要」。

### 資料源3：yfinance（ETF）— ✅ 母專案已驗
- 被動+主動ETF走此源。主動ETF代碼規則：6碼第6碼A=股票、D=債券（TWSE官方）

### 查證過但不可用/次選（附確切證據，鐵律11要求）
- 各總代理官網（安聯tw.allianzgi/野村nomurafunds）：野村=SPA(body空)、安聯=擋bot → ✗
- fundclear新站：全SPA(body空) → ✗；舊站announce：robots擋 → ✗
- 鉅亨fund.cnyes：React前端渲染 → ✗（次選）

---

## 一、鐵律（違反即重做）

1. 只動指定功能，其他code一律不碰
2. 改前必view原文；改函數必整段替換
3. 每次修改必做：ast檢查+函數定義次數=1+關鍵功能存在清單
4. 必自測附證據才交付；使用者不當測試員
5. **猜測禁止**：endpoint/HTML結構/library/資料源，一律web_search/web_fetch查證
6. 風險改動開git分支
7. 不問不等指令，主動review執行；目標勝率95%+
8. **交付固定給整份完整.py檔，不給diff/patch**（patch的index被當程式碼→SyntaxError）
9. **部署問題第一件事要log，不准推測改**（母專案血淚：pandas被升3.0.3才是真兇）
10. **主動深掘**：戳統計陷阱(n<10/倖存者偏誤)、三方視角辯證給判斷、發現漏洞主動提。
    使用者裁決後即停止爭論照做，但把已知偏誤寫進憲法/UI供歸因（照做≠當沒問題≠下次重提）
11. **【專屬｜範圍不可縮減】被動ETF+主動ETF+境內基金(含投海外)+真正境外基金，缺一不可**。
    抓不到某源必窮盡其他源+附確切失敗證據才能回報受阻。「我覺得抓不到」不算，web_fetch回應才算。
    範圍不可縮減=資料涵蓋不可縮減，不等於四類同套邏輯宣稱同等勝率。
12. **【專屬｜成本】能後補的延後，不能後補的現在做**：
    - 可延後（使用者裁決不計）：申購/贖回手續費、贖回天期、證交稅、券商費 → 參數化預設0，不寫死
    - 不可延後（資料正確性）：配息還原(鐵律14)、倖存者偏誤(鐵律15)、視窗跨度/淨值截至日(鐵律16)
    - `entry_lag` 參數化，ETF與基金均預設0（使用者裁決）；母專案`entry_price=t["curr_price"]`
13. **【專屬】查證環境限制先講**：sandbox無網路、web_fetch只能GET不能POST、
    只能抓已出現在對話的URL。需POST/XHR的來源只能驗結構層，端到端由使用者跑最小腳本回報。
14. **【配息還原】**除息日淨值跳空→10日滾動誤讀暴跌→假信號。高股息ETF尤重。用還息後報酬算。
15. **【倖存者偏誤】**已終止基金不得默默drop（SITCA註「已終止無須公告淨值」）→ 用歷史時點母體
16. **【滾動視窗】**定義維持10筆(非曆日)；記錄實際曆日跨度；跨度>25曆日該筆作廢；註記淨值截至日

---

## 二、母專案可複用資產（已實測app.py 8712行）
- `HORIZONS=[5,10,20,40,60,80,100,120,240]`(第79行)、`JOURNAL_COLS`(13欄,第85行)
- `calc_all_rolling_returns`/`run_full_backtest`(第497行)/`build_entry_timing_table`/
  `_pick_best_timing_idx`(樣本≥10→勝率最高→≤5pp比報酬→無回None)/`load_journal`/`save_journal`
- 各定義1次、AST OK（實測）
- **必改**：`entry_price=t["curr_price"]`零延遲；全檔無entry_lag概念（基金版新增，預設0）

## 三、必重新設計、不可照搬
- ❌ 六篩選條件(EPS/PB/ROE/負債比)、❌ 15分制體質評分（基金無這些財報；使用者裁決暫緩）
- ❌ 「淨值序列=可直接算報酬」（須先配息還原，鐵律14）
- ❌ 「10筆=10日」（基金非營業日各異，鐵律16）
- ❌ 母專案觸發門檻數字（基金波動被平滑+跨度浮動→門檻須先跑分布再訂）

## 四、requirements 鎖版政策（最高優先）
- pandas`>=2.3.3,<3.0`、numpy`>=2.0,<3`（pandas 3.0地雷，母專案血淚）
- reportlab必列（若沿用_pdf_*）。只精準鎖會出事的，其餘`>=`（全==會裝不起來）
- 解析SITCA/MoneyDJ需 requests（已驗）；beautifulsoup4/lxml可選（加入前Cloud實測）

## 五、實作進度與下一步

### ✅ 已完成（2026-07-20 收工，BAC三階段全數自測通過）
**檔案清單（皆在 repo：jojo164164/fund-tier1）**
| 檔案 | 行數 | 用途 | 自測 |
|------|------|------|------|
| `app.py` | 1050 | 主程式，7個tab（含☀️每早掃描） | C層8/8 + 回歸22/22 |
| `build_sitca.py` | 269 | SITCA境內建庫(36家×15年) | 7/7 |
| `build_offshore.py` | 256 | 境外雙源建庫(yfinance+MoneyDJ) | 8/8 |
| `.github/workflows/build_sitca.yml` | — | SITCA建庫Actions | YAML OK |
| `.github/workflows/build_offshore.yml` | — | 境外建庫Actions | YAML OK |

**app.py 7個tab**：☀️每早掃描 / 📊分布校準 / 🎯觸發掃描 / 🔬回測 / 📒追蹤日誌 / 🛡️系統檢核 / 🔌SITCA連線測試

**三層架構全部到位**：
- ①歷史庫：SITCA(境內)+境外雙源，斷點續傳+禮貌延遲+CSV增量+覆蓋率報告
- ②每早掃描：`load_history_db`讀CSV→`scan_history_db`算今日觸發+歷史勝率(母專案引擎)
- ③手動篩選：境內外+發行公司兩維度已上（類型/市場待第二批）

**關鍵函數（app.py，各定義1次實測）**：
- 母專案複用：calc_all_rolling_returns / run_full_backtest / build_entry_timing_table /
  _pick_best_timing_idx / load_journal / save_journal
- C層新增：load_history_db(讀歷史庫CSV,自動標境內外) / hist_to_prices(轉回測格式) /
  scan_history_db(掃描今日觸發+配歷史勝率,觸發排最前)
- 資料源：fetch_etf_list / fetch_prices(auto_adjust還原配息) / fetch_sitca_nav(兩步式POST)

**已驗證的資料源代碼格式**：
- ETF：`0050.TW`(yfinance) ｜ 主動ETF：6碼第6碼A=股/D=債
- 境內基金：SITCA(POST,投信代碼A00XX)
- 境外基金：yfinance用`0P`前綴代碼(共同基金專屬) + MoneyDJ用`?a=代碼`(Big5)補網

### ⬜ 下一步：把資料真的灌進去（工具已備齊，尚未實跑建庫）
**現況：所有腳本+Actions都做好、自測過，但歷史庫CSV是空的。**
每早掃描tab會顯示「尚無歷史庫」→ 這是預期，等下面建庫跑完就有資料。

1. **實跑SITCA建庫**：Actions→build_sitca，companies留空(全36家)、days_back填3800(15年)、
   skip_recent=2(避境外時差)。斷點續傳「按Run→等→再Run」分批完成。
2. **實跑境外建庫**：Actions→build_offshore，source=both(雙源容錯)。
   跑完看`data/offshore_coverage.csv`得知yfinance真實覆蓋率(此數字只有實跑才知)。
3. **驗證每早掃描**：歷史庫有料後，app的☀️tab會自動讀取、算觸發+勝率。
4. **篩選維度第二批**：SITCA類型代號對照表(股債平衡)、投資市場分類。
5. **配息還原(鐵律14)、倖存者偏誤(鐵律15)**：建庫穩定後接入。

## 六、給使用者的高槓桿習慣
- 回報問題附：截圖+部署log(Manage app→Download log)+失敗頁HTML
- requirements鎖版(pandas/numpy鎖2.x)｜定期下載追蹤日誌CSV備份(雲端重啟清/tmp)
- 每次貼app.py確認完整最新版｜等級B項目需本機/Actions跑回報

## 七、已否決（除非數據翻案）
部位管理❌｜同日多信號組合控管❌｜三盞燈整合❌

---

## 附錄A：SITCA 36家投信對照（VIEWSTATE解碼，2026-07驗證）
A0001兆豐 A0003第一金 A0004滙豐 A0005元大 A0006景順 A0007瀚亞 A0008玉山 A0009統一
A0010富邦 A0011摩根 A0012華南永昌 A0015瑞銀 A0016群益 A0017台中銀 A0018聯博 A0021柏瑞
A0022復華 A0025永豐 A0026中國信託 A0027宏利 A0031貝萊德 A0032野村 A0033聯邦 A0035東方匯理
A0036安聯 A0037國泰 A0038富達 A0040德銀遠東 A0041凱基 A0042施羅德 A0043街口
A0045富蘭克林華美 A0047台新 A0048合庫 A0049大華銀 A0050路博邁

## 附錄B：待查證清單（實作時補）
- [ ] SITCA「基金類型代號說明」對照表（AA/AB/AG→股票/債券/平衡）→ 篩選維度第二批
- [ ] MoneyDJ 15年歷史抓法（yp011001GIF端點或分頁參數）
- [ ] MoneyDJ 境外基金清單頁完整代碼抓取（yp081001分頁）
- [ ] SITCA IN2213配息資料（鐵律14還原用）、IN2104基金消滅（鐵律15母體用）

## 變更日誌
**2026-07-20（v4 收工總結：BAC三階段完成）**
- 依使用者選定順序 B→A→C 完成三大建置，全部自測通過：
  - **B 境外雙源建庫**：發現境外基金可用yfinance `0P`代碼抓(跟ETF同管線,實測doc86
    0P000019KV=JPM Greater China)；MoneyDJ當補網。寫`build_offshore.py`雙源容錯+覆蓋率報告，8/8過。
  - **A SITCA放大15年**：升級`build_sitca.py`→留空=全36家、skip_recent避境外時差、
    days_back參數化(3800=15年)，7/7過。原解析邏輯未破壞。
  - **C 每早掃描**：app.py加☀️tab(排最前)，讀歷史庫→算今日觸發+配歷史勝率+篩選維度。
    C層8/8 + 原功能回歸22/22。過程str_replace誤吃函數頭致語法錯，依鐵律9看確切錯誤當場修好。
- **釐清資料源分工定案**：ETF→yfinance；境內→SITCA；境外→yfinance(0P)主+MoneyDJ補。
- **MoneyDJ長歷史調查結論**：走勢圖資料在React元件(GoReactTA.aspx?SymbolID=代碼.OT)隱藏API，
  難爬易碎→改用yfinance 0P代碼(乾淨,同ETF管線)為主，不鑽MoneyDJ隱藏API。
- 三方視角確認：境外基金是盧森堡/愛爾蘭註冊國際基金,有ISIN,國際資料源(Yahoo)查得到→
  yfinance是最快達標路,不必硬幹幾十家總代理官網(SPA+擋bot)。
- **待實跑**：所有工具備齊但歷史庫還空，下次帶使用者實跑建庫灌資料。
- 使用者反饋「這檔案我要幹啥」→ 學到教訓：給後台零件時要先講清楚「現在不用動、擺著」，
  別讓使用者以為每個交付都要立刻操作。

**2026-07-20（v3 固化系統藍圖）**
- 使用者確認三層架構藍圖（歷史庫背景建/每早自動掃描/手動重掃+篩選）+四類全包
- 三大資料源全部實測可抓歷史：SITCA(建庫1438筆)、MoneyDJ(境外歷史淨值)、yfinance(ETF)
- 釐清「境內投海外≠真正境外」，使用者要三者全要；MoneyDJ確認為境外可抓集中點
- 使用者裁決：早上要快(②③)、歷史是背景參考(①)；成本不計(參數預設0)；
  entry_lag=0;「抓最新NAV標日期」不猜跳天;體質評分暫緩
- 新增篩選維度規格（兩批）；修正.gitignore擋csv的bug
- SITCA建庫技術全解：兩步式POST、雙引號解析教訓、GitHub Actions斷點續傳
**2026-07-17**：Z1完成、成本/entry_lag決議、鐵律13-16新增（詳見v2）
**2026-07-14**：建立憲法

# 憲法 v4 — 新增鐵律（2026-08-06，境外資料源那一輪的教訓）

> 來源：找境外基金長歷史那一輪。方向與突破口（cnyes、fundclear、藏 0P 的 metadata）
> 都是使用者 Greg 找到的；AI 的價值在「驗證真偽 + 落地成可靠管線」。以下鐵律據此而生。

## 鐵律17：使用者指方向，AI 負責驗證與落地，功勞歸屬要誠實
- 突破口常來自使用者的領域直覺（他知道 cnyes 有每檔歷史、他找到 fundclear）。
- AI 不得把「使用者挖到的方向」寫成自己的功勞。措辭要如實：方向是誰的、驗證是誰做的。
- AI 的真正價值 = 把一個「聽起來可行」的方向，用探針證明真偽、寫成不會半路死的碼。

## 鐵律18：外部資料源，一律「先探針、後全市場」，絕不憑印象寫全量
- 沙盒無網路，任何外部源的行為（能不能連、給幾年、擋不擋、翻不翻頁）AI 都無法確認。
- 硬寫一支「應該可以」的全市場腳本 = 重蹈覆轍（Yahoo/MoneyDJ 爬蟲那種部署才發現不能用）。
- 正確流程：小樣本探針（在 GitHub Actions 實跑）→ 拿真實數字 → 過門檻才寫全市場。
- 探針要能「診斷失敗」，不只報成敗：印出候選符號、錯誤碼、欄位全貌，才知道下一步怎麼修。

## 鐵律19：SPA 頁面的資料在背後 API，去 Network/DevTools 抓，別解析空殼 HTML
- fundclear、cnyes 都是 SPA，web 抓回來只有空殼。資料走 XHR JSON。
- 抓法：F12 → Network → Fetch/XHR →（先開工具再 F5 重整/操作）→ 找回一大串資料那筆
  → Copy as cURL / Request URL。或 Console 跑 performance.getEntries() 撈網址。
- 有了真實 API URL + 參數 + 回應格式，才寫抓取器；不要猜端點。

## 鐵律20：分頁 API 先問「有沒有一次給全部」，10筆/頁的端點在全市場規模下=不可行
- cnyes 淨值 table 端點鎖 10 筆/頁 → 全市場逐頁翻是上百萬次請求、跑幾十小時、會被封。
- 建全市場前必測：per_page/limit 加大吃不吃？有沒有 chart/bulk 端點一次回整條序列？
- 若真的只能小分頁 → 換策略（找別的欄位/端點），不要硬幹。

## 鐵律21：識別碼是「橋」——一個源缺的，用共同識別碼(ISIN/0P)接到另一個源
- 境外破局關鍵：cnyes 的 metadata 直接給了 fundClassId(=Yahoo 0P)，補上了「ISIN→0P 反查」
  一直失敗的那一段 → cnyes 拿 0P → yfinance 拿長歷史。混源靠共同 key 縫起來。
- 遇到「A 源有清單沒歷史、B 源有歷史沒清單」→ 找兩邊都有的識別碼(ISIN/0P/代碼)當橋。

## 鐵律22：免費資料源有物理極限，要誠實標示涵蓋率，不假裝 100%
- 免費、官方、全市場、多年歷史的境外淨值「客觀上不存在」（TDCC 只7天、晨星擋爬蟲、
  Bloomberg 要錢）。已窮舉查證。
- 能做到的最好 = cnyes 0P → yfinance 約 3338 檔可回測（含主流美元類股）。要對使用者講白
  「這是免費天花板、涵蓋約 N 成、其餘標資料稀疏」，不得假裝全包。
- 「窮盡查證後的次佳解」是誠實工程，不是放棄。

## 鐵律23：per-share-class 覆蓋低，用後綴/別名救援把有效涵蓋率往上推
- yfinance 對非美元平行類股的 0P 裸碼常無資料，但加交易所後綴（.F/.SG/.DE/.MU…）能救回。
- 探針顯示 60%，加後綴救援實際建出 3338 檔。落地時把「已知的救援手法」寫進建庫，別只用裸碼。

## 鐵律24：GitHub 網頁貼 workflow，name 沒變中文/自訂名 = YAML 格式壞了，重貼別硬修
- 貼 yml 時 GitHub 編輯器可能吃壞縮排/隱形字元 → 左側顯示檔名而非 name → 沒有 Run 鈕。
- 修法：用「New workflow」內建編輯器重貼（不易殘留隱形字元），name 用純英文先排除編碼問題。
- workflow 的 run: 內不要塞 heredoc python（縮排會讓結束符對不上）→ 抽成獨立 .py 檔呼叫。

# 憲法 v4 — 新增鐵律 25~29（2026-08-10，GitHub/Streamlit 平台硬限制那幾輪）

> 這幾條是「境外資料上不了 repo、app 一直當機」實際踩出來的平台限制。
> 每一條都害我們重跑過，記下來，之後建任何大資料庫前先檢查。

## 鐵律25：GitHub 單檔硬上限 100MB（建議 50MB），大資料一律切檔
- 單一檔案 >100MB → push 被「直接拒絕」；50~100MB 只是 warning（可過）。
- 症狀陷阱：commit 會成功、job 顯示綠，但 push 被拒→repo 其實沒檔。別被綠勾騙。
- 判斷：merge/commit 步的 log 要看到「remote: ... exceeds ... 100.00 MB」或
  push 沒有「main -> main」。有才是真的推上去。
- 修法：把大 CSV 切成多檔，每檔設 80MB 上限（留安全邊際）。
  ★切檔命名要帶「篩選鍵」★：若 app 要按年份省記憶體載入，檔名就要帶年份
  （offshore_nav_2025_01.csv），不能用純序號（001/002）→ 否則 app 無法只載部分。

## 鐵律26：Streamlit Community Cloud 只有 ~1GB RAM，一次全載會 OOM「Oh no」
- 症狀：app 開不了、只有「Oh no. Error running app」→ 幾乎都是記憶體爆掉。
- 實測量級：境內9年~150萬筆 + 境外3338檔~360萬筆 = 500萬筆全載 → 直接崩。
- 修法：載入要能「只讀選定年份 + 只讀必要欄位(usecols) + 重複欄位轉 category」。
  預設載「最近2年」，深度回測才短暫開大。這是主機 RAM 限制，不是資料限制。
- ★所有資料源（境內、境外、未來新增）都必須套同一套省記憶體載入★，缺一個就爆。

## 鐵律27：「資料深度」與「一次載入量」是兩件事，別混淆也別對使用者講錯
- 庫裡有幾年 ≠ app 一次能載幾年。資料全都在、都能回測；「最近2年」只是免費主機
  為了不當機的預設載入量，使用者隨時可調大（3/5/全部），極限在主機 RAM。
- 要對使用者講白：不是「只能兩年」，是「免費主機一次別載太多；要更多就升級 RAM」。

## 鐵律28：app 環境(Streamlit Cloud) ≠ 建庫環境(GitHub Actions)，連得到的源不同
- 已實測：SITCA 的 POST 從 Streamlit Cloud 回 404(被擋)、從 GitHub Actions 可成。
- 推論：「即時更新」要放在連得到源的地方=GitHub Actions 排程，不是 app 當場抓。
- 系統的「即時/最新」由後端排程維持（庫每天長新），app 只讀靜態庫→穩、不被源擋。

## 鐵律29：merge/commit 後一定要「回 repo 用眼睛確認檔案真的在」
- job 綠、log 印「寫入 N 筆」都不等於檔案在 repo（可能 push 被拒、可能 .gitignore 擋）。
- 收尾動作固定：去 GitHub repo 對應資料夾，親眼看到檔案、看大小合理，才算完成。
- 刪舊檔要 git add -A（一般 git add 不會 stage 刪除），否則舊檔殘留。

鐵律30：全站視覺一律遵循 AllianzGI 官方色碼（WCAG 達標）

主色票（HEX）：

Allianz Blue 
#003781（主色/標題/按鈕/分頁強調/表頭底）
Dark Blue 
#122B54（深底/內文）｜Grey 
#414141
Red 
#F62459（觸發/警示）｜Orange 
#F86200（高勝率≥80%強調）
Dark Green 
#00908D（正常/通過）｜Bright Green 
#5FCD8A（次強調）
藍階 Blue1~5：#006192 / #007AB3 / #13A0D3 / #B5DAE6 / #DFEFF2（圖表/背景） 規則：白底用深色字、深藍底用白/亮色字（WCAG 4.5:1）。熱圖勝率→橘系、報酬→紅綠(
#F62459/
#00908D)。 落地：.streamlit/config.toml 設 primaryColor=
#003781；表頭/熱圖用上列色。
鐵律31：icon 一律 Allianz 風（藍線 
#003781 + 圓框 + 簡約線條）
用內嵌 SVG（不依賴外部圖檔，避免載入失敗）；深藍底時 icon 轉白。
st.tabs 分頁標籤只能 emoji（Streamlit 限制）；SVG icon 放「內容區大標題」與頁首。
鐵律32：境外「發行公司/總代理」不得用基金名猜，必用官方權威來源
打地鼠教訓：手動 hand-map 每個品牌永遠補不完（CP Multi/GAM/KBI/荷寶/資本集團/歐義銳榮…）。
★根本解：總代理應「由官方資料以 ISIN 對照取得」★——TDCC 開放資料(id=3-4)每檔境外基金 都帶『總代理機構』欄，用 ISIN 把我們的基金 join 過去，直接拿官方總代理，不再猜品牌。
手動對照表只當「補充/顯示名優化」，不當唯一來源。
鐵律33：投信併購/退出要記錄，代碼不刪只標註
台灣投信會併購，SITCA 代碼可能保留歷史。已知：
A0014 新光投信（2025-11-24 併入台新投信 A0047）
A0020 日盛投信（2025-04-01 併入富邦投信 A0010）
顯示時標「(已併入XX)」，不直接刪代碼（歷史資料仍可能帶舊代碼）。
# 憲法 v4 — 新增鐵律 34~36（2026-08-14，防止重犯的血淚規範）
> 起因：AI 連續兩次嚴重犯錯 —— (A)擅自把「現貨指數」改成「期貨代理」砍需求；
>      (B)產生新 requirements.txt 時沒鎖 pandas 上限，差點覆蓋掉會導致「Oh no」。
> 這三條是「AI 自我約束」的最高優先，違反即為違憲。

## 鐵律34：需求不得擅自縮減或替換（需求完整性）
- 使用者要 A，若 A 一時做不到，**不得偷偷改成 B 交差**（例：要「現貨指數」→ 擅自給「期貨代理」）。
- 遇到「當前方法拿不到」時，正確動作是【把資料源挖到底】，非砍需求：
  1. 先找該資料的**真正來源**（JS 頁面 → 找它背後的 API/端點）。
  2. 找**替代權威源**（我們技術棧已有的優先：yfinance/cnyes/SITCA/TDCC/MoneyDJ）。
  3. 真的無解，**明講「這部分現在拿不到、原因、我試過什麼」，讓使用者決定**，不自作主張。
- 對照成功前例：NAV「SITCA雲端404」→挖到排程抓；境外「官方無歷史」→挖到cnyes→yfinance；
  AGIF/DIT「名稱分不出」→挖到TDCC ISIN。★挖源是本專案的解題法，不是砍需求。★
- 若真要用代理/近似，**必須事先講明並取得同意**，且標註「這是代理，非你要的原始資料」。

## 鐵律35：相依鎖版神聖不可侵犯（requirements 保護）
- **絕不可**產生「放寬或移除既有版本上限」的 requirements.txt。特別是：
  - `pandas>=2.3.3,<3.0`、`numpy>=2.0,<3` —— 母專案血淚，pandas 3.0 會讓 app 啟動即崩「Oh no」。
- 改 requirements 前，**必以現有檔為基準**，只做「必要的最小新增」，逐行 diff 驗證：
  【無移除任何套件、無放寬任何上限、只新增確實需要的】。
- 不確定現有內容時，**先請使用者提供現檔**，不得憑空產生覆蓋。
- 交付前自我檢查：`assert 移除==[] and 放寬==[]`。

## 鐵律36：破壞性交付前，先 diff、先自我驗證、再交付
- 任何「覆蓋既有檔」的動作，交付前必須：
  1. 對照舊版做 diff，明確列出「改了什麼、為什麼」。
  2. 能離線驗證的（語法/邏輯/欄位/相依）先驗證，附證據。
  3. 高風險項（相依、資料源、需求範圍）主動標紅請使用者確認。
- 寧可多問一句，不可擅自決定後造成 app 崩潰或需求走樣。
