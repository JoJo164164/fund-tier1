# 待辦：每早掃描「即時補最新淨值」機制

## 使用者需求（2026-07-27 辯證確認）
每早掃描時，每檔基金要用「它自己當下最新的淨值」往回算10筆：
- 境內台股基金：當天就有 → 用今天
- 境外基金：可能晚1~2天 → 用它最新的那天
- 各檔日期不對齊，標清楚「淨值截至」日即可（已內建）

## 問題根源
建庫用 skip_recent=2（避開境外殘缺日污染歷史，這是對的）
→ 但導致歷史庫最新只到「2天前」
→ 掃描時境內基金明明今天有淨值卻拿不到

## 解法（建庫完成後實作）
在 app 每早掃描「之前」加一步「即時補最新」：
1. 即時 POST SITCA 抓「最近3~5天」全市場淨值（不 skip）
2. 併進歷史庫記憶體（不寫檔，或寫增量檔）
3. 掃描時每檔自然拿到「自己最新那筆」
關鍵：這是 app 執行時即時連線，跟 GitHub Actions 建庫是不同路徑，需另測。

## 為何現在不做
- 建庫還在跑，先讓它完成
- 補最新接在掃描前，需完整歷史庫才有意義
- 需驗證 Streamlit Cloud 能即時連 SITCA（等級B→A）

---

# 待辦2：每日自動排程（2026-07-27 使用者提問觸發）

## 使用者疑慮
「每天一定要手動跑嗎？沒跑資料庫會缺嗎？」

## 正確認知（已向使用者澄清）
- SITCA 是「歷史查詢型」資料源，可查任意過去日期
- 漏幾天不會缺資料：下次跑時斷點續傳會自動補齊缺漏日期
- 與「即時快照型」（如MoneyDJ只給30天）不同，SITCA不會漏一天少一天

## 解法：GitHub Actions 定時排程
在 build_sitca.yml 加 schedule 觸發（cron），每交易日晚上自動跑補當天：
```yaml
on:
  workflow_dispatch:   # 保留手動
  schedule:
    - cron: '0 12 * * 1-5'   # 每週一~五 UTC 12:00 = 台灣 20:00
```
參數用預設（ALL、補最近幾天、skip=2）。
斷點續傳確保：某天排程失敗，下次自動補回。
使用者完全不用手動，每早打開app歷史庫已自動補到昨天。

## 為何建庫完成後才加
- 現在建庫在跑，先完成
- 排程要接在「確定能穩定跑完」的腳本上，先驗證手動跑成功
